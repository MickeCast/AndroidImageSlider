<?xml version="1.0" encoding="utf-8"?>
<!-- The tests need a real Activity to attach views to; it exists only in the test APK. -->
<manifest xmlns:android="http://schemas.android.com/apk/res/android">
    <application>
        <activity
            android:name="com.daimajia.slider.library.SliderTestActivity"
            android:exported="true"
            android:theme="@style/Theme.AppCompat.Light.NoActionBar" />
    </application>
</manifest>
